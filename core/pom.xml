<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.github.lemon-ant.jharmonizer</groupId>
        <artifactId>jharmonizer-parent</artifactId>
        <version>1.0-SNAPSHOT</version>
    </parent>
    <artifactId>jharmonizer-core</artifactId>
    <name>JHarmonizer Core</name>

    <properties>
        <!-- Minimum module branch coverage threshold. -->
        <jacoco.minimum.branch.coverage>0.70</jacoco.minimum.branch.coverage>
    </properties>

    <dependencies>
        <dependency>
            <groupId>com.palantir.javaformat</groupId>
            <artifactId>palantir-java-format</artifactId>
            <version>${palantir-java-format.version}</version>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.dataformat</groupId>
            <artifactId>jackson-dataformat-yaml</artifactId>
            <version>${jackson.version}</version>
        </dependency>
        <dependency>
            <groupId>commons-io</groupId>
            <artifactId>commons-io</artifactId>
        </dependency>
        <dependency>
            <groupId>io.github.lemon-ant</groupId>
            <artifactId>glob-path-finder</artifactId>
            <version>${glob-path-finder.version}</version>
        </dependency>
        <dependency>
            <groupId>io.github.java-diff-utils</groupId>
            <artifactId>java-diff-utils</artifactId>
            <version>4.16</version>
        </dependency>
        <dependency>
            <groupId>fr.inria.gforge.spoon</groupId>
            <artifactId>spoon-core</artifactId>
            <version>${spoon.version}</version>
            <exclusions>
                <!-- Maven model support — not used by JHarmonizer -->
                <exclusion>
                    <groupId>org.apache.maven</groupId>
                    <artifactId>maven-model</artifactId>
                </exclusion>
                <!-- Maven invoker support — not used by JHarmonizer -->
                <exclusion>
                    <groupId>org.apache.maven.shared</groupId>
                    <artifactId>maven-invoker</artifactId>
                </exclusion>
                <!--
                    commons-compress is a transitive dependency of spoon-core, but JHarmonizer does not use the
                    Spoon functionality that requires it. The project's former direct basename usage was migrated
                    from commons-compress FileNameUtils.getBaseName to commons-io FilenameUtils.
                -->
                <exclusion>
                    <groupId>org.apache.commons</groupId>
                    <artifactId>commons-compress</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>io.github.lemon-ant.jharmonizer</groupId>
            <artifactId>dependency-aware-sorting</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.openjdk.jmh</groupId>
            <artifactId>jmh-core</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.openjdk.jmh</groupId>
            <artifactId>jmh-generator-annprocess</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <configuration>
                        <!-- Keep the javac module opens while letting JaCoCo inject its late-bound test agent. -->
                        <argLine>@{argLine} ${mockito.agent.argLine}
                        --add-opens jdk.compiler/com.sun.tools.javac.api=ALL-UNNAMED
                        --add-opens jdk.compiler/com.sun.tools.javac.parser=ALL-UNNAMED
                        --add-opens jdk.compiler/com.sun.tools.javac.util=ALL-UNNAMED
                        --add-opens=jdk.compiler/com.sun.tools.javac.file=ALL-UNNAMED
                        --add-opens=jdk.compiler/com.sun.tools.javac.tree=ALL-UNNAMED</argLine>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <executions>
                    <execution>
                        <id>compile-java-test-cases</id>
                        <goals>
                            <goal>testCompile</goal>
                        </goals>
                        <phase>process-test-resources</phase>
                        <configuration>
                            <!-- Compile ONLY test-cases Java fixtures -->
                            <compileSourceRoots>
                                <compileSourceRoot>${project.basedir}/src/test/resources/test-cases</compileSourceRoot>
                            </compileSourceRoots>
                            <testIncludes>
                                <include>**/valid/**/*.java</include>
                            </testIncludes>
                            <outputDirectory>${project.build.directory}/test-cases-compile-check-classes</outputDirectory>
                            <!-- Use the test release level; fixtures intentionally include newer Java syntax. -->
                            <testRelease>${maven.compiler.testRelease}</testRelease>
                            <!-- Suppress 'strictfp' deprecation warnings from test fixtures -->
                            <compilerArgs>
                                <arg>-Xlint:-strictfp</arg>
                            </compilerArgs>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-assembly-plugin</artifactId>
                <version>${maven-assembly-plugin.version}</version>
                <!-- Keep this plugin aligned with the latest stable release used by the project -->
                <configuration>
                    <descriptors>
                        <descriptor>assembly/project-zip.xml</descriptor>
                        <!-- Assembly descriptor location -->
                    </descriptors>
                    <outputDirectory>${project.basedir}</outputDirectory>
                    <finalName>${project.artifactId}-${project.version}-sources</finalName>
                    <!-- Output ZIP name, e.g. myproject-1.0-sources.zip -->
                    <appendAssemblyId>false</appendAssemblyId>
                    <!-- Do not append the assembly id suffix to the filename -->
                </configuration>
                <executions>
                    <execution>
                        <id>make-assembly</id>
                        <!-- Bind to package phase: after compilation/tests, before install -->
                        <goals>
                            <goal>single</goal>
                        </goals>
                        <phase>package</phase>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>benchmark-sort</id>
            <properties>
                <benchmark.forks>0</benchmark.forks>
                <benchmark.iterations>10</benchmark.iterations>
                <benchmark.measurementBatchSize>1000</benchmark.measurementBatchSize>
                <benchmark.threads>4</benchmark.threads>
                <benchmark.warmupIterations>5</benchmark.warmupIterations>
            </properties>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>exec-maven-plugin</artifactId>
                        <version>3.6.1</version>
                        <configuration>
                            <classpathScope>test</classpathScope>
                            <mainClass>org.openjdk.jmh.Main</mainClass>
                            <arguments>
                                <argument>.*SortingAlgorithmBenchmark.*</argument>
                                <argument>-foe</argument>
                                <argument>true</argument>
                                <argument>-tu</argument>
                                <argument>ms</argument>
                                <argument>-bm</argument>
                                <argument>avgt</argument>
                                <argument>-wi</argument>
                                <argument>${benchmark.warmupIterations}</argument>
                                <argument>-i</argument>
                                <argument>${benchmark.iterations}</argument>
                                <argument>-f</argument>
                                <argument>${benchmark.forks}</argument>
                                <argument>-jvmArgsAppend</argument>
                                <argument>--add-opens=java.base/java.lang=ALL-UNNAMED</argument>
                                <argument>-jvmArgsAppend</argument>
                                <argument>--add-opens=java.base/java.util=ALL-UNNAMED</argument>
                                <argument>-t</argument>
                                <argument>${benchmark.threads}</argument>
                                <argument>-p</argument>
                                <argument>measurementBatchSize=${benchmark.measurementBatchSize}</argument>
                                <argument>-rf</argument>
                                <argument>json</argument>
                                <argument>-rff</argument>
                                <argument>${project.basedir}/sorting-benchmark.json</argument>
                            </arguments>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
