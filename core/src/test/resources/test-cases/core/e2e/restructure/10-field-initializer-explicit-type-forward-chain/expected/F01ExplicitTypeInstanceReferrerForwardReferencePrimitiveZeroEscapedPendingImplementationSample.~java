package io.github.lemon_ant.jharmonizer.core.e2e;

// Escaped fixture (.~java): intentionally excluded from automated e2e execution.
// This shows a potentially unsafe reordered shape produced without the missing dependency edge.
public class F01ExplicitTypeInstanceReferrerForwardReferencePrimitiveZeroEscapedPendingImplementationSample {
    private int aaa = F01ExplicitTypeInstanceReferrerForwardReferencePrimitiveZeroEscapedPendingImplementationSample.bravo + 1;
    private static int zzz = new F01ExplicitTypeInstanceReferrerForwardReferencePrimitiveZeroEscapedPendingImplementationSample().aaa;
    private static int bravo = 10;

    public static void main(String[] args) {
        // This assertion should still hold after harmonization; currently this shape can violate it.
        if (zzz != 1 || bravo != 10) {
            throw new IllegalStateException("Unexpected values: zzz=" + zzz + ", bravo=" + bravo);
        }
    }
}
