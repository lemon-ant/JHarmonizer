fix-imports: true
formatter-style: PALANTIR # NONE, GOOGLE, PALANTIR

top-level-types-ordering:
  main-type-first: true
  type-groups:
    - [class, record]
    - interface
    - enum
    - annotation
  intra-group-sorting: alpha
